import rospy
from std_msgs.msg import String
def main():
rospy.init_node('pick_place_demo')
pub = rospy.Publisher('/demo/status', String, queue_size=1)
rate = rospy.Rate(1)
for i in range(5):
pub.publish(f"step {i}")
rate.sleep()


if __name__ == '__main__':
main()
